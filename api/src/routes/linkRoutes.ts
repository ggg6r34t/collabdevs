import express from "express";
// import {
//   createLink,
//   getLinks,
//   upvoteLink,
//   downvoteLink,
// } from "../controllers/linkController";
// import { isAuthenticated } from "../controllers/authController";

const router = express.Router();

// router.get("/links", getLinks);
// router.post("/links", isAuthenticated, createLink);
// router.put("/links/:linkId/upvote", isAuthenticated, upvoteLink);
// router.put("/links/:linkId/downvote", isAuthenticated, downvoteLink);

export default router;
